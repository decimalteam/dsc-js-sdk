export interface DecimalContract {
    id: string;
    name: string;
    address: string;
    implementation: string;
}

export interface DecimalBridgeContract {
    id: string;
    implementation: string;
}